apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5002

